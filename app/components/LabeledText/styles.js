import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  root: {
    alignItems: 'center',
  },

  label: {
    color: '#EE2A7B',
    fontSize: 16,
    fontWeight: '100',
    marginBottom: 5,
  },

  value: {
    textAlign: 'center',
    fontSize: 16,
  },
});

export default styles;
